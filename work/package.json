{
  "name": "runehold-td",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build --outDir ../outputs/game --emptyOutDir",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:all": "npm run test && npm run build && npm run test:e2e"
  },
  "dependencies": {
    "vite": "^8.0.16"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "vitest": "^4.1.8"
  }
}
